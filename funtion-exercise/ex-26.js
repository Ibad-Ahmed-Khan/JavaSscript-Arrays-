// Given an array of strings, return a new array that contains the length of each string.
let arr = ["ibad", "ahmed", "khan", "pahan", "lala", "batan"];
let string__length = arr.map((arr) => arr.length);
console.log(string__length);
